# spike/

**I1 bare end-to-end only.** Load real weights → prefill → decode steps →
prove non-salad (`docs/COHERENCE-TRAPS.md` §4).

Throwaway-by-default: Python is allowed here for trap-debug speed. Do not
promote into `crates/` without I2 productization. Record every run under
`../runs/…-spike/` with the trap checklist signed.
